{
  "name": "copiloto-comercial",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,backend -c blue,green \"npm run dev -w web\" \"npm run dev -w backend\"",
    "dev:web": "npm run dev -w web",
    "dev:backend": "npm run dev -w backend",
    "build": "npm run build -w web && npm run build -w backend",
    "lint": "npm run lint -w web"
  },
  "devDependencies": {
    "concurrently": "^9"
  }
}
